<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/activity_rules"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/guides_horizontal_padding"
    android:paddingLeft="@dimen/guides_vertical_padding"
    android:paddingRight="@dimen/guides_vertical_padding"
    android:paddingTop="@dimen/guides_horizontal_padding"
    tools:context="ch.epfl.sweng.jassatepfl.RulesActivity">

    <Space
        android:id="@+id/rules_top_space"
        android:layout_width="match_parent"
        android:layout_height="?android:attr/actionBarSize"
        android:layout_alignParentTop="true"/>

    <ScrollView
        android:id="@+id/rules_layout"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_below="@id/rules_top_space">

        <com.mukesh.MarkdownView
            android:id="@+id/rules_text"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"/>

    </ScrollView>

</RelativeLayout>
